#ifndef CONVERSION_H
#define CONVERSION_H

#include <iostream>
#include <string>
#include <sstream>

using namespace std;

class conversion
{
    public:
        conversion();
        virtual ~conversion();
        string numToString(int num);
    protected:
    private:
};

#endif // CONVERSION_H
